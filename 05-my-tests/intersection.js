function intersection(nums1, nums2) {
  if(!Array.isArray(nums1) || nums1.length < 1)
    throw new Error(`First argument of intersection function has to be an array with length >= 1`);
  if(!Array.isArray(nums2) || nums2.length < 1)
    throw new Error(`Second argument of intersection function has to be an array with length >= 1`);

  
}

module.exports = {
  intersection
};